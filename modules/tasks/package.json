{
  "name": "@toolkit/tasks",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@toolkit/auth": "workspace:*",
    "@toolkit/db": "workspace:*",
    "@toolkit/tenant": "workspace:*",
    "better-auth": "catalog:",
    "@hono/zod-validator": "catalog:",
    "drizzle-orm": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/better-sqlite3": "catalog:",
    "better-sqlite3": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
